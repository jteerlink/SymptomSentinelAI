import SwiftUI

/// A simplified theme manager placeholder
/// Theme functionality has been removed from the app, this is kept as a minimal
/// implementation to maintain compatibility with existing code references
class ThemeManager: ObservableObject {
    /// Singleton instance
    static let shared = ThemeManager()
    
    /// Default initializer
    private init() {}
}